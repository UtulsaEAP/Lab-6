def process_user_contacts(contact_list, contact_name):
    
    # Put word pairs into a dictionary
    # user_contacts = 
    
    # Put word pairs into a dictionary

    # Return the dictionary value for the contact name
    return 
if __name__ == '__main__':
    # Get input for word pairs
    user_input = input("Enter word pairs (name, phone number): ")
    # Replace the commas with spaces
    # user_input = 
    # Split the string into separate words
    # user_input = 
    
    # Get the name to look up
    contact_name = input("Enter the contact name: ")
    
    # Call the function to process user contacts
    print(process_user_contacts(user_input, contact_name))
